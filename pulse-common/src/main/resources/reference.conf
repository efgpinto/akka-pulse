# pulse-common library defaults. Services override in their own application.conf.
# A library must ship reference.conf, never application.conf (it would shadow the service's own).

# Identifies the serving service in the shared /pulse/health response.
pulse.health.service-name = "unknown-service"
pulse.health.version = "1.0-SNAPSHOT"
