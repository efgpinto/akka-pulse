name: Snyk Recommended Scans

on:
  workflow_dispatch:
    inputs:
      runtime_version:
        description: 'Akka runtime version (e.g. 1.6.15)'
        required: true
        type: string

jobs:
  # Scans only /opt/docker/akka/lib/ from the runtime image — the JARs actually
  # loaded at runtime for SDK 3.1+, with the sbt dependencyOverrides already
  # applied. Ground truth, no false positives from legacy Kalix libs.
  snyk-runtime-image-scan-akka-only:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build scan image (Akka runtime libs only, excludes legacy Kalix libs)
        run: |
          cat > /tmp/Dockerfile.scan <<EOF
          FROM registry.akka.io/akka-runtime-postgres:${{ inputs.runtime_version }} AS source
          FROM eclipse-temurin:21-jre-jammy
          COPY --from=source /opt/docker/akka/lib/ /opt/docker/akka/lib/
          EOF
          docker build --platform linux/amd64 -f /tmp/Dockerfile.scan -t akka-runtime-scan:${{ inputs.runtime_version }} .

      - name: Install Snyk CLI
        run: npm install -g snyk

      - name: Snyk scan Akka runtime libs
        run: snyk container test akka-runtime-scan:${{ inputs.runtime_version }} --severity-threshold=medium
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  # Scans exactly the dependency list shipped inside the
  # akka-runtime-dependencies JAR (META-INF/akka-runtime-dependencies.properties,
  # the same manifest the SDK enforcer uses). Each entry becomes a direct
  # dependency with wildcard exclusions, so no transitives are resolved and no
  # stale versions can be falsely flagged.
  snyk-runtime-manifest-scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Configure Akka repository
        uses: ./.github/actions/configure-akka-repo
        with:
          akka-repo-url: ${{ secrets.AKKA_REPO_URL }}

      - name: Generate manifest pom
        run: |
          RUNTIME_VERSION="${{ inputs.runtime_version }}"
          mkdir -p /tmp/runtime-manifest-scan
          cd /tmp/runtime-manifest-scan

          # Fetch the runtime-dependencies JAR; its META-INF manifest lists the exact
          # version of every dependency shipped in the runtime image.
          mvn -q org.apache.maven.plugins:maven-dependency-plugin:3.6.1:get \
            -Dartifact=io.akka:akka-runtime-dependencies:${RUNTIME_VERSION}:jar
          JAR=~/.m2/repository/io/akka/akka-runtime-dependencies/${RUNTIME_VERSION}/akka-runtime-dependencies-${RUNTIME_VERSION}.jar

          # Every manifest entry becomes a direct dependency with wildcard exclusions,
          # so the resolved graph is exactly the manifest: flat, no transitives.
          unzip -p "$JAR" META-INF/akka-runtime-dependencies.properties \
            | awk -F'[%=]' '!/^#/ && /=/ {printf "      <dependency>\n        <groupId>%s</groupId>\n        <artifactId>%s</artifactId>\n        <version>%s</version>\n        <exclusions>\n          <exclusion>\n            <groupId>*</groupId>\n            <artifactId>*</artifactId>\n          </exclusion>\n        </exclusions>\n      </dependency>\n", $1, $2, $3}' > deps.xml

          {
            cat <<'POMHEAD'
          <project xmlns="http://maven.apache.org/POM/4.0.0"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                                https://maven.apache.org/xsd/maven-4.0.0.xsd">
            <modelVersion>4.0.0</modelVersion>
            <groupId>com.example</groupId>
            <artifactId>runtime-manifest-scan</artifactId>
            <version>1.0.0</version>
            <packaging>pom</packaging>
            <dependencies>
          POMHEAD
            cat deps.xml
            cat <<'POMTAIL'
            </dependencies>
          </project>
          POMTAIL
          } > pom.xml

      - name: Resolve manifest dependencies
        run: mvn -f /tmp/runtime-manifest-scan/pom.xml dependency:resolve -q

      - name: Install Snyk CLI
        run: npm install -g snyk

      - name: Snyk scan runtime manifest dependencies
        run: snyk test --file=/tmp/runtime-manifest-scan/pom.xml --severity-threshold=medium
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  # snyk test on the project's own pom.xml, as one would do for a regular Maven
  # application, with issues introduced via io.akka:akka-runtime-dev filtered
  # out of the results (local development runtime, never deployed). Snyk has no
  # native "ignore everything from this dependency" rule (.snyk ignores are
  # per-vulnerability ID), so the job post-filters Snyk's JSON output on the
  # dependency path instead.
  # Note: this job does not use the runtime_version input.
  snyk-naive-pom-scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Configure Akka repository
        uses: ./.github/actions/configure-akka-repo
        with:
          akka-repo-url: ${{ secrets.AKKA_REPO_URL }}

      - name: Install Snyk CLI
        run: npm install -g snyk

      - name: Snyk scan application pom (ignoring akka-runtime-dev)
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: |
          set +e
          snyk test --severity-threshold=medium --json > snyk-result.json
          code=$?
          set -e
          # 0 = no vulns, 1 = vulns found, >=2 = scan error
          if [ "$code" -ge 2 ]; then
            cat snyk-result.json
            exit "$code"
          fi

          ignored=$(jq '[.vulnerabilities[] | select(.from | map(startswith("io.akka:akka-runtime-dev")) | any)] | length' snyk-result.json)
          remaining=$(jq '[.vulnerabilities[] | select((.from | map(startswith("io.akka:akka-runtime-dev")) | any) | not)] | length' snyk-result.json)
          echo "Ignored $ignored issue paths introduced via io.akka:akka-runtime-dev"
          echo "Remaining issue paths: $remaining"
          jq -r '.vulnerabilities[] | select((.from | map(startswith("io.akka:akka-runtime-dev")) | any) | not) | "✗ [\(.severity)] \(.id) in \(.packageName)@\(.version)\n  introduced by \(.from | join(" > "))"' snyk-result.json
          [ "$remaining" -eq 0 ]
