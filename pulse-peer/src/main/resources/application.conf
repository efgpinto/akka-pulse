# Pulse peer configuration.
# On-demand service that validates service-to-service settings against pulse-core.

# Identity reported by the shared health endpoint from pulse-common (feature 004).
pulse.health.service-name = "pulse-peer"
